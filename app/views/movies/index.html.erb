<ul>
  <% @movies.each do |movie| %>
    <li>
      <%= movie.title %>
      <%= movie.overwiew %>
      <%= movie.poster_url %>
      <%= movie.raing %>
      <%= movie.submit %>
    </li>
  <% end %>
</ul>
